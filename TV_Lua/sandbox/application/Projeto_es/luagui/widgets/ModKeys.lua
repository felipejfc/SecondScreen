--- Module containing ModKeys enum.
require(SB_REQUIRE_LUAGUI .. "widgets.enum")

--- Enum ModKeys defining modifier keys. Those can be:
--  <code>SHIFT, NONE</code> and <code>SYMBOL</code>.
ModKeys = enum { 'SHIFT', 'NONE',  'SYMBOL' }

