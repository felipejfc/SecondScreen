--- Module containing enum about Layout parameters.
require(SB_REQUIRE_LUAGUI .. "widgets.enum")

--- Layout parameters enum defining: FILL_PARENT, FIXED_SIZE and WRAP_CONTENT.
LayoutParams = enum {
    'FILL_PARENT',
    'FIXED_SIZE',
    'WRAP_CONTENT'
}

